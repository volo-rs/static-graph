node E -> (X, Y) {
    #[default = "crate::Cache::new"]
    cache: crate::Cache,
}

node X -> O {
    x: list<string>,
}

node Y -> O {
    y: map<i32, string>,
}

node O {
    o: string,
}

graph G(E)
